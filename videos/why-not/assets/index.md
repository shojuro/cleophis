# Asset ledger — why-not

| role | file | provenance |
| ---- | ---- | ---------- |
| type (regular) | `fonts/georgia.ttf` | Copied at build time from the host's licensed Windows font store (`/mnt/c/Windows/Fonts/georgia.ttf`). NOT committed to git (Microsoft-licensed; no redistribution). To re-render on another host, re-copy from a licensed Windows install: `cp /mnt/c/Windows/Fonts/georgia*.ttf assets/fonts/`. Fallback stack `'Times New Roman', serif` renders acceptably per BRIEF. |
| type (italic) | `fonts/georgiai.ttf` | Same source (`georgiai.ttf`). The headline "why not?" is set in true Georgia Italic. |
| background | — | No footage provider in this environment (media-use has no video type; heygen CLI absent; no GEMINI/GOOGLE keys). The nuclear-explosion background is a hand-authored deterministic GSAP layer stack — see `shot-plan.json` and `index.html`. |
| audio | — | Intentionally silent (spec itemized visuals only; recorded in BRIEF.md). |
