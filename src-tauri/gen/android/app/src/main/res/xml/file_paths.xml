<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
  <external-path name="my_images" path="." />
  <cache-path name="my_cache_images" path="." />
  <!-- Self-update (spec §4.1): the downloaded, hash-verified APK lives in the
       app's internal <files>/updates/ dir; FileProvider grants the package
       installer a content:// URI for ACTION_VIEW. See app_update.rs (Phase 4). -->
  <files-path name="app_updates" path="updates/" />
</paths>
