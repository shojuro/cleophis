{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core IPC, events, path resolution",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
